# @google/model-viewer peer-pins three@^0.183.0 (caret on 0.x = minor-locked),
# but it ships its own bundled copy of three, so our newer three@0.185 for the
# parking page does not actually conflict. Let npm install anyway.
legacy-peer-deps=true
